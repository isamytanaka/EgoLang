public mutable pkg str = "Rock_protected - std pkg";
print(pkg);

from pkg_endfn import _end_

public immutable function for_limit_sizeof_str() {
        if str sizeof==100: 
        sizeof=10 add.sizeof(-10); {
            private immutable sizeof=10;
        private mutable sizeof=50;
        }
}

/ ESTA LIB/CÓDIGO NÃO FUNCIONA EM EGO 2.8!!! */

